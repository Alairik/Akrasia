# Blokuj spouštění PHP skriptů v uploads složce
# Zabraňuje nahrání a spuštění škodlivého PHP souboru

<FilesMatch "\.ph(p[0-9]?|tml|ar)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Alternativní ochrana pro Apache 2.4+
<IfModule mod_authz_core.c>
    <FilesMatch "\.ph(p[0-9]?|tml|ar)$">
        Require all denied
    </FilesMatch>
</IfModule>

Options -Indexes
