RewriteEngine On
RewriteBase /akrasia/admin/

# Serve existing files directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything to admin front controller
RewriteRule ^ index.php [L,QSA]
